# JRETS Controller — 产品需求文档

**项目名称**：JRETS Controller
**副标题**：JR East Train Simulator 外设控制插件
**版本**：v1.2（草稿）
**目标平台**：Windows 10 / 11
**开发语言**：Python 3.x + Pygame
**适配游戏**：JR East Train Simulator

---

## 1. 背景与目标

### 1.1 背景

JR East Train Simulator（以下简称 JRETS）仅支持通过键盘控制油门/制动档位，每次操作只能移动一个档位，且不提供任何第三方设备接口或按键自定义功能。

用户希望通过标准 USB 手柄/摇杆的模拟轴直接控制档位，实现更接近真实驾驶的操作体验。

### 1.2 游戏按键定义

JRETS 的键盘操作固定如下，不可更改：

| 按键 | 功能 |
|---|---|
| `Q` | 降低一个档位（制动方向） |
| `Z` | 升高一个档位（动力方向） |
| `S` | 直接跳转到空档 N |
| `.`（句号） | 紧急制动 EB |

### 1.3 解决思路

开发一个独立的 Windows 外置插件，核心逻辑为：

1. 读取外设轴的实时位置，折算为目标档位整数
2. 对比目标档位与插件维护的当前档位状态，计算差量
3. 向游戏模拟发送对应数量的 `Q` / `Z` 按键，驱动游戏档位追上目标值
4. 支持将外设按钮映射到 `S`（直跳空档）和 `.`（EB 紧急制动）
5. 可选：轴推到末端时触发一次 EB
6. 检测游戏窗口是否处于前台聚焦状态，未聚焦时暂停所有按键发送

### 1.4 目标

- 支持标准 USB 手柄/摇杆作为输入设备
- 支持不同车辆的档位结构（含 HB 档位的车辆）
- 提供 Pygame GUI 界面，实时显示档位状态并支持配置操作
- 打包为单个 `.exe` 文件，无需用户安装 Python 环境

---

## 2. 档位结构

### 2.1 档位类型与顺序

档位从低到高的完整顺序为：

```
BN → B(N-1) → … → B1 → HB → N → P1 → … → PN  [→ EB 轴末端区域（可选）]
```

| 类型 | 说明 | 是否所有车辆都有 | 颜色 |
|---|---|---|---|
| B1 ~ BN | 常用制动档，数量因车型而异 | 是 | 蓝色 |
| HB | 保持制动档（Hold Brake），位于 B1 和 N 之间 | 否 | 橙色 |
| N | 空档，`S` 键直跳 | 是 | 绿色 |
| P1 ~ PN | 动力档，数量因车型而异 | 是 | 橙色 |
| EB | 紧急制动，不在普通档位序列内，独立触发 | 是 | 红色（激活时全界面变红） |

### 2.2 颜色系统

档位颜色在条状图和右侧文字显示中保持一致，当前档位切换时文字颜色同步变化：

| 档位类型 | 条状图颜色 | 右侧文字颜色 |
|---|---|---|
| 制动档 B | 蓝色 | 蓝色 |
| 保持制动 HB | 橙色 | 橙色 |
| 空档 N | 绿色 | 绿色 |
| 动力档 P | 橙色 | 橙色 |
| 紧急制动 EB | 红色 | 红色 |

### 2.3 档位内部表示

将 EB 以外的所有档位线性化为从 0 开始的整数序列，EB 独立处理不参与序列。

以含 HB 的车型（B4 + HB + N + P4）为例，共 10 档：

| 内部值 | 档位名称 | 颜色 |
|---|---|---|
| 0 | B4 | 蓝色 |
| 1 | B3 | 蓝色 |
| 2 | B2 | 蓝色 |
| 3 | B1 | 蓝色 |
| 4 | HB | 橙色 |
| 5 | N | 绿色 |
| 6 | P1 | 橙色 |
| 7 | P2 | 橙色 |
| 8 | P3 | 橙色 |
| 9 | P4 | 橙色 |

### 2.4 车辆配置

通过 `vehicles.json` 配置每种车辆的档位结构：

```json
{
  "E231系_通勤": {
    "power_notches": 5,
    "brake_notches": 8,
    "has_hb": false
  },
  "某型车_含HB": {
    "power_notches": 4,
    "brake_notches": 4,
    "has_hb": true
  },
  "E5系_新干线": {
    "power_notches": 5,
    "brake_notches": 7,
    "has_hb": false
  }
}
```

`total_notches` 计算方式：

```
total_notches = brake_notches + (1 if has_hb else 0) + 1(N) + power_notches
```

切换车辆后内部状态重置为未同步，需重新执行归位同步。

### 2.5 档位名称生成

根据配置动态生成档位名称列表，顺序从低到高（内部值 0 → max）：

```python
def build_notch_names(power, brake, has_hb):
    names = []
    for i in range(brake, 0, -1):   # BN ~ B1
        names.append(f"B{i}")
    if has_hb:
        names.append("HB")
    names.append("N")
    for i in range(1, power + 1):   # P1 ~ PN
        names.append(f"P{i}")
    return names

def notch_color(name):
    if name.startswith("B"):  return COLOR_BLUE
    if name == "HB":          return COLOR_ORANGE
    if name == "N":           return COLOR_GREEN
    if name.startswith("P"):  return COLOR_ORANGE
    if name == "EB":          return COLOR_RED
```

---

## 3. 功能需求

### 3.1 外设读取

#### 3.1.1 设备枚举与选择

- 程序启动时自动检测所有已连接的 USB 手柄/摇杆设备
- 在 UI 中以列表形式展示设备名称，用户可选择目标设备
- 支持运行时切换设备，无需重启程序

#### 3.1.2 轴配置

- 提供**轴监视模式**：实时列出所有轴的编号与当前数值，变化幅度最大的轴自动高亮，点击即可设为目标控制轴
- 支持**反转轴**选项（向前推 = 高档 / 低档，可配置）
- 支持**死区**配置，默认 ±0.05，低于阈值视为零，消除摇杆抖动

#### 3.1.3 轴值到档位的映射

用户可自定义轴值与档位的对应关系，支持两种模式：

**模式 A：均匀线性映射（默认）**

```
normalized = (-axis_value + 1.0) / 2.0   # 前推为高档，值域 0.0 ~ 1.0
target_notch = round(normalized * (total_notches - 1))
```

**模式 B：自定义分段映射**

用户在 UI 中手动标定：将轴拨到某个物理位置，点击"设为 N 档"，插件记录该轴值对应 N 档；同理标定其他关键档位。其余档位在相邻标定点之间线性插值。标定数据存入 `config.json` 的 `axis_calibration` 字段。

#### 3.1.4 轴末端 EB（可选）

用户可在配置中开启"轴末端 EB"功能，并设定触发阈值（默认轴值 > 0.95）：

- 轴值超过阈值时，触发一次 `.` 键，UI 显示 EB 状态
- 轴值退出阈值区域后不做任何操作，EB 状态解除，恢复正常档位显示
- 再次进入阈值区域才会重新触发一次

```json
"axis_eb_enabled": true,
"axis_eb_threshold": 0.95
```

#### 3.1.5 按钮映射

用户可将手柄按钮映射到以下功能，每个功能独立配置，通过"按钮监视模式"绑定：

| 功能 | 触发行为 | 游戏按键 |
|---|---|---|
| 空档（N） | 清空队列，发送一次 `S` 键，`current_notch` 同步到 N 的内部值 | `S` |
| 紧急制动（EB） | 清空队列，发送一次 `.` 键，UI 显示 EB 状态，不修改 `current_notch` | `.` |
| 归位同步 | 触发归位同步流程 | — |

### 3.2 按键模拟

#### 3.2.1 按键对应关系

| 操作 | 模拟按键 |
|---|---|
| 升高一个档位 | `Z` |
| 降低一个档位 | `Q` |
| 直跳空档 N | `S` |
| 紧急制动 EB | `.`（句号） |

#### 3.2.2 发送机制

使用 `ctypes.windll.user32.SendInput` 模拟键盘按下+松开。

采用**待发队列**机制避免阻塞 Pygame 主循环：差量计算完成后将所有待发按键加入队列，主循环每帧从队列取出一个按键发送。

```
pending_keys = []

# 每帧执行：
if pending_keys and (now - last_key_time) > KEY_INTERVAL:
    send_key(pending_keys.pop(0))
    last_key_time = now
```

**EB 为最高优先级**：无论来自按钮还是轴末端，触发时立即清空待发队列，优先发送 `.` 键。

#### 3.2.3 S 键直跳 N 档的状态同步

按下 N 档按钮时：
1. 清空当前待发队列
2. 发送一次 `S` 键
3. 将 `current_notch` 直接设为 N 档对应的内部值
4. 下一帧正常读取轴值，若轴不在 N 位置则重新计算差量

#### 3.2.4 发送间隔

相邻两次按键发送间隔默认 **100ms**，可在配置中调整。

### 3.3 初始状态同步

程序启动时内部状态为**未同步**（`None`），此时外设轴输入不触发按键发送，仅 EB 按钮/轴末端 EB 有效。

同步流程：
1. 用户在游戏内手动将档位拨到最低（BN 档）
2. 按手柄上配置的归位按钮，或点击 UI 中的"归位同步"按钮
3. 插件发送 `total_notches + 2` 次 `Q` 键，确保游戏档位到底
4. 将内部状态设为 `0`，同步完成，外设轴控制开始生效

### 3.4 窗口焦点检测

每个主循环周期检测当前前台窗口：

- 通过 `win32gui.GetForegroundWindow()` 获取前台窗口句柄
- 通过句柄查询 PID，再通过 PID 查询进程名是否匹配目标 exe
- 备选：匹配窗口标题字符串

**未聚焦时**：清空待发队列，不发送任何按键，UI 显示"未聚焦"状态。  
**重新聚焦后**：重新计算当前轴值对应的目标档位，恢复正常控制。

---

## 4. UI 需求

### 4.1 技术实现

使用 **Pygame** 渲染 UI，通过 `pywin32` 将窗口设为 Always On Top，悬浮于游戏上方。

```python
hwnd = win32gui.FindWindow(None, "JRETS Controller")
win32gui.SetWindowPos(hwnd, win32con.HWND_TOPMOST, 0, 0, 0, 0,
                      win32con.SWP_NOMOVE | win32con.SWP_NOSIZE)
```

### 4.2 窗口布局

窗口尺寸约 **400 × 560px**，固定大小，分三个区域：

```
┌──────────────────────────────────────┐
│  车辆: 某型车_含HB   设备: Xbox手柄  │  ← 顶部状态栏
│  游戏: ● 已聚焦      同步: ● 已同步  │
├─────────────┬────────────────────────┤
│             │                        │
│  竖向档位   │   [橙色] HB            │  ← 主显示区
│  条状图     │   保持制动              │
│             │   4 / 10               │
├─────────────┴────────────────────────┤
│  [选择车辆]   [选择设备]  [归位同步]  │  ← 操作区
│  主轴: 1  ☐ 反转   [轴监视模式]      │
│  N档按钮: 3   EB按钮: 0              │
│  发送间隔: 100ms  ☐ 轴末端EB  [标定] │
└──────────────────────────────────────┘
```

### 4.3 主显示区

**左侧：竖向条状档位图**

条从上到下对应档位从高到低，各区段颜色：

| 区段 | 颜色 |
|---|---|
| 动力区（P1 ~ PN） | 橙色 |
| 空档（N） | 绿色（单格） |
| 保持制动（HB，如有） | 橙色 |
| 制动区（B1 ~ BN） | 蓝色 |

当前档位以高亮块标记对应格。EB 激活时条状图全部变红，并在右侧显示 EB 状态。轴末端 EB 区域在条状图顶部额外渲染一个红色小区块（仅当"轴末端 EB"开启时显示）。

**右侧：文字显示**

- 大字显示当前档位名称（`B3` / `HB` / `N` / `P2` / `EB`），**文字颜色与当前档位类型对应的颜色一致**
- 中字显示档位描述（"制动 3 档" / "保持制动" / "空档" / "动力 2 档" / "紧急制动"），同色
- 小字显示内部索引（如 `4 / 10`，EB 激活时不显示）

### 4.4 状态栏指示灯

| 状态 | 显示 |
|---|---|
| 游戏窗口已聚焦 | ● 绿色 |
| 游戏窗口未聚焦 | ● 灰色 |
| 已完成归位同步 | ● 绿色 |
| 未同步（禁止输入）| ● 红色 |

### 4.5 轴监视模式

开启后主显示区替换为所有轴的实时数值列表，变化最大的轴自动高亮，点击即设为目标轴并退出模式。

---

## 5. 主循环逻辑

```
每帧（30Hz）执行：
│
├─ pygame.event.pump()
│
├─ 检测 EB（最高优先级）
│   ├─ EB 按钮按下，或轴值 > eb_threshold（且上帧未在阈值内）
│   └─ → 清空队列，发送 '.' 键，UI 显示 EB，跳过本帧轴处理
│
├─ 检测 N 档按钮
│   └─ 按下 → 清空队列，发送 'S' 键，current_notch 设为 N 的内部值
│
├─ 检测游戏窗口焦点
│   ├─ 未聚焦 → 清空待发队列，更新 UI，跳过本帧剩余逻辑
│   └─ 已聚焦 → 继续
│
├─ 读取主轴值，应用死区与反转，映射为 target_notch
│
├─ 若 current_notch 为 None（未同步）→ 跳过
│
├─ 若 target_notch != current_notch 且待发队列为空
│   └─ 计算差量，填充 'Z'（升）或 'Q'（降）到待发队列，更新 current_notch
│
├─ 若待发队列非空 且 距上次发送 > KEY_INTERVAL
│   └─ 发送队列头部一个按键
│
└─ 渲染 UI，clock.tick(30)
```

---

## 6. 配置文件

### 6.1 `config.json`（运行时配置，自动保存）

```json
{
  "selected_vehicle": "某型车_含HB",
  "joystick_index": 0,
  "axis_index": 1,
  "axis_invert": false,
  "deadzone": 0.05,
  "key_interval_ms": 100,
  "poll_hz": 30,
  "button_neutral": 3,
  "button_eb": 0,
  "button_resync": 7,
  "axis_mapping_mode": "linear",
  "axis_calibration": {},
  "axis_eb_enabled": false,
  "axis_eb_threshold": 0.95
}
```

### 6.2 `vehicles.json`（车辆档位定义）

```json
{
  "E231系_通勤": {
    "power_notches": 5,
    "brake_notches": 8,
    "has_hb": false
  },
  "某型车_含HB": {
    "power_notches": 4,
    "brake_notches": 4,
    "has_hb": true
  },
  "E5系_新干线": {
    "power_notches": 5,
    "brake_notches": 7,
    "has_hb": false
  }
}
```

---

## 7. 依赖与打包

### 7.1 Python 依赖

```
pygame
pywin32
psutil
pyinstaller
```

### 7.2 打包命令

```bash
pyinstaller --onefile --noconsole \
  --add-data "vehicles.json;." \
  --name "JRETS_Controller" \
  main.py
```

`config.json` 在首次运行时自动生成于 exe 同目录，`vehicles.json` 随 exe 一同分发。

---

## 8. 建议开发顺序

1. **轴监视模式**：仅打印轴值，确认轴编号与方向，不涉及任何游戏交互
2. **档位映射验证**：加入 `build_notch_names()` 与 `notch_color()`，打印目标档位名称与颜色，不发按键
3. **按键发送测试**：在记事本中测试 `Q`/`Z`/`S`/`.` 发送效果，确认队列机制正常
4. **EB 与 N 档按钮**：实现高优先级 EB 清队列逻辑、轴末端 EB 触发、S 键直跳同步
5. **焦点检测集成**：验证切出/切回游戏时的暂停与恢复
6. **归位同步逻辑**：在游戏中验证档位对齐
7. **Pygame UI**：搭建界面，接入主循环，实现档位颜色与文字颜色联动
8. **多车辆与 HB 支持**：验证含 HB 车辆的档位顺序、名称生成与条状图分段渲染
9. **轴标定功能**：实现自定义分段映射的 UI 标定流程
10. **打包测试**：PyInstaller 打包，在无 Python 环境的机器上验证